# ACV → Vote RN : dépendances Python
# Installation : pip install -r requirements.txt
# Python >= 3.11 requis

# Manipulation données
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0       # lecture/écriture parquet
openpyxl>=3.1.0       # lecture Excel
scipy>=1.10.0

# Machine learning / matching
scikit-learn>=1.3.0   # logistic regression, imputation, StandardScaler

# Économétrie DiD
differences>=0.3.0    # Callaway-Sant'Anna (2021) — ATTgt
pyfixest>=0.20.0      # TWFE, Sun-Abraham (sunab), clustering
linearmodels>=4.30    # panel OLS alternatif
statsmodels>=0.14.0   # OLS, tests statistiques

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0

# Cartographie (optionnel, pour Carte 1)
geopandas>=0.13.0

# Utilitaires
thefuzz>=0.19.0       # fuzzy matching pour codes communes
requests>=2.28.0      # téléchargements HTTP
